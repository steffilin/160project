import React from 'react';
import '../styles/main.css';

function LoginPage() {
  return (
    <div>
      <h2>Login to RunLink</h2>
      <button>Login with Google</button>
      <button>Login with Facebook</button>
    </div>
  );
}

export default LoginPage;
